{
  "name": "fabledemo",
  "displayName": "fabledemo",
  "description": "",
  "version": "0.0.1",
  "publisher": "Chris",
  "engines": {
    "vscode": "^1.5.3"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.pasteSelection"
  ],
  "main": "./out/extension",
  "keybindings": [
    {
      "command": "extension.pasteSelection",
      "key": "shift+ctrl+x"
    } 
  ],
  "contributes": {
    "commands": [
      {
        "command": "extension.pasteSelection",
        "title": "SimpleRepl: PasteSelection"
      }
    ]
  },

  "scripts": {
    "build": "fable demo.fsproj --outDir ./out -m commonjs --sourceMaps --verbose"
  },
  "devDependencies": {
    "fable-compiler": "^0.6.9"
  },
  "dependencies": {
    "fable-core": "^0.6.7"
  }
}
