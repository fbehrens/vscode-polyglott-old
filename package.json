{
  "name": "fabledemo",
  "displayName": "fabledemo",
  "description": "",
  "version": "0.0.1",
  "publisher": "Chris",
  "engines": {
    "vscode": "^0.10.10"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.sayHello"
  ],
  "main": "./out/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.sayHello",
        "title": "Hello World"
      }
    ]
  },
  "scripts": {
    "build": "fable extension.fsx --outDir ./out -m commonjs --verbose"
  },
  "devDependencies": {
    "fable-compiler": "^0.6.9",
    "vscode": "^1.0.0"
  },
  "dependencies": {
    "fable-core": "^0.6.7"
  }
}
